# ملخص مشروع منصة الخدمات الموحدة (HelpDesk)

## نظرة عامة
منصة مركزية لإدارة الخدمات الإلكترونية للمؤسسة الملكية للأعمال الإنسانية، تهدف لأتمتة تقديم ومتابعة واعتماد الطلبات عبر مسارات عمل ديناميكية (Workflows).

## البنية التقنية (Architecture)
- **إطار العمل:** Next.js (App Router) v15+
- **قاعدة البيانات:**Supabase (PostgreSQL)
- **اللغة:** TypeScript
- **التصميم:** Tailwind CSS, Shadcn UI (Clean Blue Theme)
- **الميزات الرئيسية:**
    - دعم اللغة العربية (RTL).
    - محرك سير عمل مرن (Workflow Engine).
    - منشئ نماذج ديناميكي (Form Builder).

## الحالة الحالية (Current Status)
- ✅ **جاهز:**
    - الأساسيات (المصادقة، الهيكل).
    - إنشاء الطلبات وحفظ البيانات.
    - دورة سير العمل الأساسية (Draft -> Manager Approval -> Approved/Rejected).
    - واجهة المسؤول (Admin Dashboard).
- 🛠 **قيد العمل:**
    - نظام الإشعارات (Notifications).
    - التسلسل الإداري (Manager Chain).
    - التحقق المتقدم من النماذج.

## الملفات الرئيسية
- `src/lib/workflow-engine.ts`: محرك سير العمل.
- `src/app/actions.ts`: إجراءات السيرفر (Server Actions).
- `PROJECT_ROADMAP.md`: خارطة الطريق.

## ملاحظات للمستقبل
- يجب إكمال نظام الإشعارات لضمان تجربة مستخدم متكاملة.
- لوحة تحكم المستخدم تحتاج إلى تفعيل البيانات الحقيقية.
