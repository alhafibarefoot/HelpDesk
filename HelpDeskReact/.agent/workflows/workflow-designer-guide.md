---
description: دليل استخدام مصمم سير العمل - شرح جميع الأدوات
---

# 📘 دليل استخدام مصمم سير العمل

## 🎯 نظرة عامة
مصمم سير العمل (Workflow Designer) هو أداة مرئية لبناء وإدارة سير العمل للخدمات المختلفة. يتيح لك إنشاء مخططات تدفق معقدة بسهولة باستخدام السحب والإفلات.

---

## 🧰 صندوق الأدوات (Toolbox)

### 📁 القسم الأول: أساسي (Basic Nodes)

#### 🟢 1. بداية (Start Node)
- **الوظيفة**: نقطة البداية لأي سير عمل
- **الاستخدام**: 
  - كل سير عمل يجب أن يحتوي على عقدة بداية واحدة فقط
  - تُمثل نقطة دخول الطلب إلى النظام
- **الخصائص**:
  - العنوان/التسمية فقط
- **مثال**: "بداية طلب إجازة"

#### 🔴 2. نهاية (End Node)
- **الوظيفة**: نقطة النهاية لسير العمل
- **الاستخدام**:
  - يمكن أن يكون هناك أكثر من عقدة نهاية (نهايات متعددة)
  - تُحدد نتيجة سير العمل
- **الخصائص**:
  - **نوع النهاية**: 
    - ✓ مكتمل (Completed)
    - ✗ مرفوض (Rejected)
    - ⊘ ملغي (Cancelled)
    - ↪️ محول (Redirected)
    - ⏸️ معلق (On Hold)
    - ⏱️ منتهي (Expired)
  - **السبب**: نص اختياري يوضح سبب النهاية
- **مثال**: "نهاية - موافق على الإجازة"

---

### 📋 القسم الثاني: إجراءات (Action Nodes)

#### 🔵 3. نموذج (Form Node)
- **الوظيفة**: عرض نموذج للمستخدم لإدخال البيانات
- **الاستخدام**:
  - جمع معلومات من المستخدم
  - تعديل بيانات الطلب
- **الخصائص**:
  - العنوان/التسمية
- **مثال**: "نموذج تفاصيل الإجازة"
- **ملاحظة**: النموذج يتم تصميمه في صفحة منفصلة (Form Builder)

#### 🟣 4. موافقة (Approval Node)
- **الوظيفة**: طلب موافقة من شخص أو دور معين
- **الاستخدام**:
  - موافقة المدير
  - موافقة الموارد البشرية
  - موافقة المالية
- **الخصائص**:
  - **الدور المسؤول**: من سيقوم بالموافقة (موظف، مدير، مشرف، HR، مالية)
  - **وقت الإنجاز (SLA)**: المدة المتوقعة بالساعات
  - **تنبيه عند (%)**: إرسال تنبيه عند الوصول لنسبة معينة من الوقت (افتراضي 80%)
  - **التصعيد بعد (ساعات)**: تصعيد تلقائي للمدير إذا تجاوز الوقت
  - **ساعات العمل فقط**: احتساب ساعات العمل فقط (8 صباحاً - 5 مساءً)
  - **معالجة الأخطاء**:
    - إعادة المحاولة عند الفشل
    - عدد المحاولات (1-5)
    - التأخير بين المحاولات (بالثواني)
    - مسار بديل عند الفشل
    - إشعار المسؤول عند الخطأ
- **مثال**: "موافقة المدير المباشر"

#### 🟠 5. إجراء (Action Node)
- **الوظيفة**: تنفيذ إجراء تلقائي (إرسال بريد، تحديث قاعدة بيانات، إلخ)
- **الاستخدام**:
  - إرسال إشعارات
  - تحديث البيانات
  - استدعاء API خارجي
- **الخصائص**: نفس خصائص عقدة الموافقة
- **مثال**: "إرسال بريد إلكتروني للموظف"

---

### 🔀 القسم الثالث: منطق (Logic Nodes)

#### 🟡 6. بوابة (Gateway Node)
- **الوظيفة**: تقسيم المسار إلى عدة مسارات متوازية
- **الاستخدام**:
  - تنفيذ عدة إجراءات في نفس الوقت
  - اتخاذ قرارات بناءً على شروط
- **أنواع البوابات**:
  - **AND**: يجب إكمال جميع المسارات قبل المتابعة
  - **OR**: يكفي إكمال أي مسار واحد للمتابعة
  - **XOR**: مسار واحد فقط يتم تنفيذه (حسب الشرط)
- **الخصائص**:
  - العنوان (AND أو OR أو XOR)
- **مثال**: "بوابة AND - موافقة المدير والموارد البشرية معاً"

#### 🔵 7. دمج (Join Node)
- **الوظيفة**: دمج عدة مسارات متوازية مرة أخرى في مسار واحد
- **الاستخدام**:
  - بعد استخدام بوابة AND لدمج المسارات
  - تجميع نتائج عدة إجراءات متوازية
- **الخصائص**:
  - العنوان فقط (افتراضي: JOIN)
- **مثال**: "دمج - بعد موافقة جميع الأطراف"

#### 🟣 8. سير فرعي (Sub-Workflow Node)
- **الوظيفة**: استدعاء سير عمل آخر كجزء من السير الحالي
- **الاستخدام**:
  - إعادة استخدام سير عمل موجود
  - تقسيم سير عمل معقد إلى أجزاء أصغر
- **الخصائص**:
  - **مفتاح الخدمة (Service Key)**: مفتاح الخدمة الفرعية المراد استدعاؤها
- **مثال**: "سير فرعي - موافقة أمنية"

---

## 🔗 الروابط (Edges/Connections)

### كيفية إنشاء رابط:
1. انقر على نقطة الخروج (دائرة صغيرة) في العقدة المصدر
2. اسحب إلى نقطة الدخول في العقدة الهدف
3. سيتم إنشاء رابط متحرك (animated) بينهما

### خصائص الرابط:
- **العنوان/التسمية**: وصف للرابط (مثل: "موافق"، "مرفوض")
- **شرط الانتقال (اختياري)**:
  - **الحقل**: اسم الحقل في البيانات (مثل: action, amount)
  - **المعامل**: 
    - يساوي (=)
    - لا يساوي (≠)
    - أكبر من (>)
    - أصغر من (<)
  - **القيمة**: القيمة المطلوبة للمقارنة

**مثال**: 
- الحقل: `action`
- المعامل: `يساوي`
- القيمة: `approve`
- النتيجة: ينتقل إلى هذا المسار فقط إذا كانت قيمة `action` تساوي `approve`

---

## 🎨 كيفية استخدام المصمم

### 1. إضافة عقدة جديدة:
- انقر على الأداة المطلوبة من صندوق الأدوات
- ستظهر العقدة في مكان عشوائي على اللوحة
- اسحبها إلى المكان المطلوب

### 2. تعديل خصائص العقدة:
- انقر على العقدة
- ستظهر لوحة الخصائص على اليسار
- عدّل الخصائص المطلوبة
- التغييرات تُحفظ تلقائياً

### 3. ربط العقد:
- انقر على نقطة الخروج (الدائرة السفلية) في العقدة المصدر
- اسحب إلى نقطة الدخول (الدائرة العلوية) في العقدة الهدف

### 4. حذف عقدة أو رابط:
- انقر على العقدة أو الرابط
- في لوحة الخصائص، انقر على زر "حذف الخطوة"

### 5. ترتيب تلقائي:
- انقر على زر "ترتيب تلقائي" في شريط الأدوات العلوي
- سيتم ترتيب جميع العقد بشكل منظم تلقائياً

### 6. حفظ المخطط:
- انقر على زر "حفظ المخطط" في شريط الأدوات العلوي
- سيتم التحقق من صحة المخطط قبل الحفظ

---

## ✅ قواعد التحقق (Validation Rules)

عند حفظ المخطط، يتم التحقق من:
1. **عقدة بداية واحدة فقط**: يجب أن يكون هناك عقدة بداية واحدة فقط
2. **عقدة نهاية واحدة على الأقل**: يجب أن يكون هناك عقدة نهاية واحدة على الأقل
3. **جميع العقد متصلة**: يجب أن تكون جميع العقد متصلة ببعضها

---

## 📝 أمثلة عملية

### مثال 1: سير عمل بسيط لطلب إجازة

```
[بداية] 
   ↓
[نموذج - تفاصيل الإجازة]
   ↓
[موافقة - المدير المباشر]
   ↓ (موافق)
[إجراء - إرسال بريد للموظف]
   ↓
[نهاية - مكتمل]
```

### مثال 2: سير عمل متقدم مع بوابة

```
[بداية]
   ↓
[نموذج - تفاصيل الطلب]
   ↓
[بوابة AND]
   ↓ (مسار 1)          ↓ (مسار 2)
[موافقة - المدير]  [موافقة - الموارد البشرية]
   ↓                    ↓
[دمج JOIN]
   ↓
[إجراء - تحديث قاعدة البيانات]
   ↓
[نهاية - مكتمل]
```

### مثال 3: سير عمل مع شروط

```
[بداية]
   ↓
[نموذج - طلب شراء]
   ↓
[بوابة XOR]
   ↓ (المبلغ < 1000)          ↓ (المبلغ >= 1000)
[موافقة - المدير]          [موافقة - المدير المالي]
   ↓                            ↓
[نهاية - مكتمل]            [نهاية - مكتمل]
```

---

## 🚀 نصائح وأفضل الممارسات

1. **ابدأ بالبساطة**: ابدأ بسير عمل بسيط ثم أضف التعقيد تدريجياً
2. **استخدم أسماء واضحة**: اجعل أسماء العقد والروابط واضحة ومفهومة
3. **اختبر السير**: جرّب السير على طلب تجريبي قبل تفعيله
4. **استخدم القوالب**: احفظ سير العمل كقالب لإعادة استخدامه
5. **وثّق القرارات**: استخدم الأوصاف لتوثيق سبب كل خطوة
6. **راجع SLA**: تأكد من أن أوقات SLA واقعية ومناسبة
7. **استخدم التصعيد**: فعّل التصعيد التلقائي للخطوات الحرجة

---

## 🔧 استكشاف الأخطاء

### المشكلة: لا يمكن حفظ المخطط
- **الحل**: تحقق من وجود عقدة بداية واحدة وعقدة نهاية واحدة على الأقل

### المشكلة: الرابط لا يعمل
- **الحل**: تأكد من أن الشرط صحيح والحقل موجود في البيانات

### المشكلة: العقد متداخلة
- **الحل**: استخدم زر "ترتيب تلقائي" لإعادة ترتيب العقد

---

## 📚 مصادر إضافية

- **القوالب**: استخدم مكتبة القوالب للحصول على أمثلة جاهزة
- **المساعد الذكي**: استخدم المساعد الذكي لإنشاء سير عمل تلقائياً
- **التوثيق**: راجع التوثيق الكامل للنظام

---

**تم إنشاء هذا الدليل بواسطة Antigravity AI Assistant** 🤖
