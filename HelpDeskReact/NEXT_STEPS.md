# خطوات إصلاح مشكلة حذف/أرشفة الخدمات

للأسف، المشكلة الرئيسية هي أن **النظام يمنع التعديل أو الحذف** لأنه يتطلب "مفتاح مدير النظام" (Service Role Key) الذي تأكدنا من عدم وجوده في ملف الإعدادات لديك.

### الحل (عندما تعود غداً):

1. **الحصول على المفتاح:**
   - اذهب إلى لوحة تحكم Supabase الخاصة بمشروعك website.
   - Project Settings -> API.
   - انسخ مفتاح `service_role` (secret).

2. **إضافته للمشروع:**
   - افتح ملف `.env.local` في هذا المجلد.
   - أضف سطر جديد: `SUPABASE_SERVICE_ROLE_KEY=المفتاح_الذي_نسخته`

3. **إعادة التشغيل:**
   - أوقف السيرفر وشغله مرة أخرى (`npm run dev`).

بمجرد القيام بذلك، **ستعمل خاصية الحذف والأرشفة فوراً** لأن هذا المفتاح يعطي النظام الصلاحية الكاملة لتجاوز أي عوائق.

تصبح على خير! 😴
